# Counter-Strike 2 Data Analysis

In this repo I will be doing work with CS2 game data.

## Setup

This project uses a [conda](https://docs.conda.io/) environment named `cs2` (Python 3.11).

```sh
conda create -n cs2 python=3.11
conda activate cs2
pip install -r requirements.txt
```